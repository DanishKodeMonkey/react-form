import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'
import './index.css'

//The index handles the final deployment of the app on the DOM

//Render the app component on the root of the DOM
ReactDOM.render(<App />, document.getElementById('root'))